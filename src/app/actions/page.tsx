// Vault Core — Approved Execution Engine (Actions, Phase 9.0).

import { VaultActions } from "@/components/core/VaultActions";

export const metadata = {
  title: "Actions · Vault Core Execution Engine",
};

export default function ActionsPage() {
  return <VaultActions />;
}

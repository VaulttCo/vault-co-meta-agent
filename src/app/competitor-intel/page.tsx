// Vault Core — Competitor Intelligence (Valentina market signals, Phase 8.4).

import { CompetitorIntel } from "@/components/core/CompetitorIntel";

export const metadata = {
  title: "Competitor Intel · Valentina · Vault Core",
};

export default function CompetitorIntelPage() {
  return <CompetitorIntel />;
}

// Vault Core — Content Ideas + Creative Brief Builder, Draft Mode (Phase 9.7).

import { CreativeBriefs } from "@/components/core/CreativeBriefs";

export const metadata = {
  title: "Creative Briefs · Vault Core",
};

export default function CreativeBriefsPage() {
  return <CreativeBriefs />;
}

// Vault Core — Draft Approval Queue (Phase 6, Veronica).

import { DraftApprovalQueue } from "@/components/core/DraftApprovalQueue";

export const metadata = {
  title: "Draft Approval Queue · Vault Core",
};

export default function DraftsPage() {
  return <DraftApprovalQueue />;
}

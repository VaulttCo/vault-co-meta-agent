// Vault Core — Finance / Invoice Action Builder, Draft Mode (Phase 9.6).

import { FinanceDrafts } from "@/components/core/FinanceDrafts";

export const metadata = {
  title: "Finance Drafts · Vault Core",
};

export default function FinanceDraftsPage() {
  return <FinanceDrafts />;
}

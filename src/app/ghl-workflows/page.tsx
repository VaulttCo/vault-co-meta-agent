// Vault Core — GHL Workflow Builder, Draft Mode (Phase 9.3).

import { GHLWorkflows } from "@/components/core/GHLWorkflows";

export const metadata = {
  title: "GHL Workflow Drafts · Vault Core",
};

export default function GHLWorkflowsPage() {
  return <GHLWorkflows />;
}

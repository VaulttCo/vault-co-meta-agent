// Vault Core — Lead Reply + Client Message Drafting, Draft Mode (Phase 9.4).

import { MessageDrafts } from "@/components/core/MessageDrafts";

export const metadata = {
  title: "Message Drafts · Vault Core",
};

export default function MessageDraftsPage() {
  return <MessageDrafts />;
}

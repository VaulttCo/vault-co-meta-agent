// Vault Core — Meta Campaign Action Builder, Draft Mode (Phase 9.5).

import { MetaCampaignDrafts } from "@/components/core/MetaCampaignDrafts";

export const metadata = {
  title: "Meta Campaign Drafts · Vault Core",
};

export default function MetaCampaignDraftsPage() {
  return <MetaCampaignDrafts />;
}

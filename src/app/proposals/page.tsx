// Vault Core — System Creation Engine V1 proposals (Phase 3).

import { SystemProposals } from "@/components/core/SystemProposals";

export const metadata = {
  title: "System Proposals · Vault Core",
};

export default function ProposalsPage() {
  return <SystemProposals />;
}

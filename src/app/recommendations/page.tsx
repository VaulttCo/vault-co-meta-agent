// Vault Core — Recommendations (Command Hub Integration, Phase 2).

import { VaultCoreRecommendations } from "@/components/core/VaultCoreRecommendations";

export const metadata = {
  title: "Vault Core Recommendations · Command Hub",
};

export default function RecommendationsPage() {
  return <VaultCoreRecommendations />;
}

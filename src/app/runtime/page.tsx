// Vault Core — Runtime Activity (Continuous Operations, read-only).

import { RuntimeActivity } from "@/components/core/RuntimeActivity";

export const metadata = {
  title: "Runtime Activity · Vault Core",
};

export default function RuntimePage() {
  return <RuntimeActivity />;
}

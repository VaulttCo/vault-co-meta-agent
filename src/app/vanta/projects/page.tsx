// VANTA — projects list + create (V1).

import { VantaProjects } from "@/components/vanta/VantaProjects";

export const metadata = { title: "Projects · Vanta" };

export default function VantaProjectsPage() {
  return <VantaProjects />;
}

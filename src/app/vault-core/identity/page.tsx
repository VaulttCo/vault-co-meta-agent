// Vault Core — Vault Co Identity Core (Phase 6.8).

import { IdentityView } from "@/components/core/IdentityView";

export const metadata = {
  title: "Vault Co Identity Core · Vault Core",
};

export default function IdentityPage() {
  return <IdentityView />;
}

// Vault Core — Workforce dashboard (Phase 3).

import { WorkforceView } from "@/components/core/WorkforceView";

export const metadata = {
  title: "Workforce · Vault Core",
};

export default function WorkforcePage() {
  return <WorkforceView />;
}
